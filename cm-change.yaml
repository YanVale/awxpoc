---
- name: Set ConfigMap on k8s
  hosts: local
  gather_facts: yes
  vars_files:
    - kubectl_vars.yml
  collections:
    - community.kubernetes

  tasks:
    
    #- name: Set Facts
      #set_fact:
        #path: "{{ lookup('file',playbook_dir file=configmap.yaml }}"
        
     - name: Export namespace
     command: export {{NS}}
     tag: exportns  

    - name: Run Kubectl config Context
     command: kubectl config set-context --current --namespace={{NS}}
     tag: k8s_config_context
     
     #- name: Export var
     #command: export {{msvariable}}
     #tag: exportvars
     
   #- name: Update Configmap
     #command: envsubst < {{path}} | kubectl apply -f -
     #tag: setconfigmap
     
   - name: Start Trigger Pipeline gitlab
     command: curl --request POST --form token=9981c9b238f3555dde7d1d6d9d164f --form ref=devdeploy-wip-add-variables3 --form "variables[{{msvariable}}]={{propertie}}" "https://gitlab.corp.sigga.com/api/v4/projects/189/trigger/pipeline"
     tag: triggergitlab
     
     
